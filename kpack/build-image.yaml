apiVersion: kpack.io/v1alpha2
kind: ClusterBuilder
metadata:
  name: heroku-builder
spec:
  tag: heroku/builder:24
  stack:
    name: base
    kind: ClusterStack
  store:
    name: default
    kind: ClusterStore
