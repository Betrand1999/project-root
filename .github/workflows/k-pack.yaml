name: Deploy with Kpack

on:
  push:
    branches:
      - main
      - new_feature_branch

jobs:
  kpack-deploy:
    runs-on: [macOS, X64]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Build image with Cloud Native Buildpacks
        run: |
          pack build my-app \
            --builder heroku/builder:24 \
            --path . \
            --publish \
            --tag betrand1997/my-app:latest
